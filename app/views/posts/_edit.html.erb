<%= form_for [@story, @post], :html => { :style => "margin: 0" }, :remote => true do |f| %>
  <%= f.hidden_field :parent_id %>
  <%= f.text_area :message, :rows => 5, :class => 'span5' %><br />
  <%= f.submit :class => 'btn btn-primary', :value => "Edit" %>
<% end %>