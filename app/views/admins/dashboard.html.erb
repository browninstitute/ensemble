<div class="preferences row-fluid">
  <%= render 'admins/left_nav' %>

  <div class="preferences-content span9">
    <h1>Dashboard</h1>
    
    <div class="preferences-inner">
      <h2>Send Announcement to All Users</h2>
      <p>This is a dangerous form! Once you send a message, you will not be able to cancel it, so make absolutely sure it is correct. This form is also currently unable to handle HTML.</p>
      <%= form_tag(:controller => "admins", :action => "send_announcement", :method => :post, :html => { :class => "form-horizontal" } ) do %>
        <div class="control-group">
          <label class="control-label" for="name">
            <%= label_tag :subject %> 
          </label>
          <div class="controls">
            <%= text_field_tag :subject, nil, :class => "input-large" %>
          </div>
        </div>
        <div class="control-group">
          <label class="control-label" for="message">
            <%= label_tag :message %> 
          </label>
          <div class="controls">
            <%= text_area_tag :message, nil, :class => "input-large" %>
          </div>
        </div>
        <div class="control-group">
          <div class="controls">
            <%= submit_tag "Send Announcement", :class => "btn" %>
          </div>
        </div>
      <% end %>

    </div>
  </div>
</div>
