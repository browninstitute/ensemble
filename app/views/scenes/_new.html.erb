<% if @story.new_record? || scene.new_record? || can?(:edit, scene) %>
  <%= link_to new_or_edit_scene_path(scene), :class => "new-scene span4", :remote => true do %>
    <%= t 'scenes.new.create_scene' %>
  <% end %>
<% end %>
